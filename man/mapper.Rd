% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mapper.R
\name{mapper}
\alias{mapper}
\title{Plots data onto a map}
\usage{
mapper(mapdata, ...)
}
\arguments{
\item{mapdata}{data frame. A user-specific data frame with coordinates.}

\item{...}{Additional parameters:
\itemize{
\item\code{color} character string vector indicating, which color is assigned to each string. It is prioritized over colors based on the column \code{color}.
\item\code{regions} numeric. Specifies the level of administrative borders. By default \code{0} for displaying only country borders.
\item\code{title} character string. Text for the title of the plot.
\item\code{legend_title} character string. Text for the title of the legend. It is prioritized over titles based on column \code{group}.
\item\code{show_legend} logical. If \code{TRUE}, a legend with all unique strings in  the column \code{group} will be displayed, provided there is a column \code{group}. If \code{FALSE}, no legend will be displayed. By default, \code{TRUE}.
\item\code{frame} data frame. Sets the frame of the map.
\item\code{plot_size} numeric. Specifies the value by which the size of the map is scaled.
}}
}
\value{
A plot.
}
\description{
This function plots a user-specific data frame onto a map.
}
\details{
This function's purpose is to allow users to provide data frames by the function \code{top()}, edited ones as well as own data frames.

The data frame must have \emph{at least} two columns called \code{latitude} & \code{longtitude}.

Data frames output by the function \code{top()} consist of, among others, a \code{latitude}, \code{longitude}, \verb{country code} and \code{group} column.

If the input data frame has a column \code{color}, the function will assign every value in that column to the respective coordinates. However, if specified, the additional parameter \code{color} will be used instead of the column \code{color} (see above).

If the input data frame has a column \code{group}, the function will group data and display a legend.

If the input data frame has a \code{color} and a \code{group} column, the assignment must match each other. Every \code{group} (every unique string in that column) must be assigned a unique color throughout the data frame.

If \code{regions}  is set to a value greater than \code{0}, the data frame must have a column \verb{country code}.

Parameter \code{frame} accepts data frames containing coordinates which define the frame of the plot. The data frame must have a column called \code{latitude} & a column called \code{longitude}. The latitudinal and longitudinal ranges define the frame of the plot.

Parameter \code{plot_size} accepts numeric values of greater than -1. The plot's size is scaled by the given value. Thus, a value of 0 extends the size by 0\%. A value of .1 extends the size by 10\%. A value of -.1 reduces the size by 10\% and so on.
}
\examples{
## We recommend setting a persistent path for downloaded data by using toponymOptions()
## Users can always set the path manually when a function is used
## For illustration purposes, the path is manually set each time in the following examples:
\donttest{
mapper(top("itz$", "DE", toponym_path = tempdir()))
# returns a plot with all populated places
# in Germany ending in "itz"


UG_data <- top(c("et$", "wa$"), "UG", toponym_path = tempdir())
UG_data$color <- "blue"
UG_data[UG_data$group == "wa", "color"] <- "grey"
mapper(UG_data, legend_title = "two strings", title = "Some locations in grey and blue")
# returns a plot with all populated places
# in Uganda ending in "wa" (grey) and "et" (blue)
# the plot is titled "Some locations in grey and blue"
# the legend title is "two strings"
}

}
